<nav class="container mx-auto flex justify-center flex-wrap md:flex-nowrap">
  <% if user_signed_in? %>
  <div class="mb-4 md:mb-0">
    <span class="font-medium text-xl">
      <%= "Hello, #{current_user&.bride} & #{current_user&.bridegroom}" %>
    </span>
  </div>
  <div>
      <%= link_to '編輯新人資料', edit_user_path(current_user.username), class:"btn-outline-dark" %>
      <%= link_to '祝福列表', comments_path(current_user.username), class:"btn-outline-dark" %>
      <%= link_to '登出', sign_out_sessions_path,method: "delete", class: "btn-outline-dark" %>
  </div>
  <% end %>
</nav>
